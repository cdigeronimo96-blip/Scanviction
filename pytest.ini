[pytest]
testpaths = tests
addopts = -q --tb=short
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
# Quiet the Streamlit bare-mode "missing ScriptRunContext" logging during app import.
log_cli = false
