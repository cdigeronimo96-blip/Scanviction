# Upper bound is DELIBERATE: five embeds (Stripe checkout, OneSignal + native Web Push
# subscribe, and the two scroll-to-top helpers) require st.components.v1.html's
# same-origin srcdoc iframe. Streamlit warns it will be REMOVED after 2026-06-01, and
# its replacement (st.iframe, data: URL = opaque origin) breaks all five — parent-page
# access, Stripe.js, the OneSignal SDK and service-worker registration all need the
# page's real origin. Verified working on 1.59. Bump the ceiling only after testing
# checkout + push + scroll on the new version.
streamlit>=1.40,<1.60
pandas
numpy
plotly
ta
extra-streamlit-components
# Optional: powers the opt-in "Auto-refresh prices" toggle on Discover (a clean
# full-page refresh on a timer). Guarded import — app works without it.
streamlit-autorefresh
scikit-learn
requests
# Lightweight rule-based sentiment scorer for news headlines (free social-sentiment
# hybrid: ApeWisdom buzz + company-news direction). Pure-python, no model download.
vaderSentiment
# Postgres driver — REQUIRED for durable account storage via DATABASE_URL.
# Without this, DATABASE_URL silently falls back to ephemeral file storage,
# and accounts will NOT survive a Streamlit Cloud reboot/redeploy.
psycopg[binary]>=3.1
# Stripe — REQUIRED for in-page checkout / subscriptions. Without it, clicking a
# paid plan shows "stripe library not installed".
stripe>=9.0
# bcrypt — REQUIRED for salted, slow password hashing (replaces legacy sha256).
bcrypt>=4.0
# websocket-client — used by self_kick.py (Render sidecar) to open a real local
# Streamlit session at boot so the scan worker starts without any visitor.
websocket-client>=1.6
# pywebpush — native (self-hosted) Web Push with VAPID keys. Replaces OneSignal:
# no external service, no SDK script, pushes sent straight from our alert pipeline.
pywebpush>=1.14
