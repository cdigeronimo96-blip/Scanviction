requests
yfinance
pandas
ta
# Postgres driver — REQUIRED so the worker shares the app's DATABASE_URL store.
# Without it, the worker falls back to local files it can't share with the app.
psycopg[binary]>=3.1
